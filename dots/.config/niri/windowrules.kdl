// Specifying which windows should be opened maximized
window-rule {
  open-maximized false
  open-maximized-to-edges false
  open-fullscreen false
  default-column-width { proportion 0.5; }

  match app-id=r#"kitty"#
  match app-id=r#"yazi"#
  match app-id=r#".*inkscape.*"#
  match app-id=r#".*zen.*"#
  match app-id=r#".*libresprite.*"#
  match app-id=r#".*equibop.*"#
  match title=r#".*GitHub.*"#
  match title=r#".*Claude.*"#
  match title=r#".*YouTube.*"#
  match title=r#".*GitHub.*"#
  match title=r#".*WhatsApp.*"#
}
window-rule {
  open-maximized true

  match app-id=r#"nvim"# 
}

// Borderless + shadow
window-rule {
  match is-active=true 
  focus-ring {
    on 
    width 6
  }

  border {
    off 
  }

  shadow {
    on
    softness 1 
    spread 6
    offset x=8 y=8
    draw-behind-window false
    color "#000000"
  }
}

// Floaties
window-rule {
  open-floating true

  match app-id=r#"float.kitty"#
  match app-id=r#"float.yazi"#
  match app-id=r#"float.rmpc"#
  match app-id=r#".*cachyos.hello.*"#
  match app-id=r#".*Thunderbird.*"#
  match title="Noctalia"
  match title="terminal filechooser"
}
window-rule {
  match title="terminal filechooser"

  default-window-height { fixed 500; } 
  default-column-width  { fixed 900; }
}
window-rule {
  match title="Library" app-id="zen"
  match app-id="yazi"

  open-floating true
  default-window-height { fixed 600; } 
  default-column-width  { fixed 900; }
}
window-rule {
  match app-id="dev.noctalia.Noctalia.Settings"
  open-floating true
  default-column-width { fixed 1080; }
  default-window-height { fixed 920; }
}


// Blur on inactive windows
window-rule {
  match is-active=false
  
  opacity 0.85
  background-effect {
    blur true
    xray false
  }
}
// Blur config
blur {
  passes 3        // more passes = stronger blur (default: 3)
  offset 3.0      // sample distance per pass (default: 3.0)
  noise 0.01      // grain overlay (default: 0.02)
  saturation 1.0  // color saturation boost (default: 1.5)
}
